package day50SeleniumGrid;

public class SeleniumGridVM {
	
	//running it in local and making it as HUB and NODE (Local is HUB, node should be other but I'm assigning it to my local as NODE)
	//download the seleniumGrid from selenium dev
	//run this command in cmd :>java -jar "C:\Workspace\selenium-server-4.25.0.jar" standalone
	//grid set up url standalone : http://10.0.0.142:4444/ui/
	
	
	
}
